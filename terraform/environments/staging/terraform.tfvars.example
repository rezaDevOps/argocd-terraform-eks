# Core Configuration
aws_region   = "us-west-2"
project_name = "argocd-terraform"

# GitOps Configuration - PLEASE UPDATE THESE VALUES
gitops_repo_url = "https://github.com/your-org/gitops-repo.git"
domain_name     = "staging.example.com"

# EKS Addons (All enabled for staging)
enable_aws_load_balancer_controller = true
enable_external_dns                 = true
enable_cert_manager                  = true
enable_cluster_autoscaler           = true
enable_metrics_server              = true
enable_prometheus                  = true  # Enable monitoring in staging

# IAM Users (optional - add your team members)
aws_auth_users = [
  # {
  #   userarn  = "arn:aws:iam::123456789012:user/staging-user"
  #   username = "staging-user"
  #   groups   = ["system:masters"]
  # }
]

# Note: The following are set automatically for staging environment:
# - cluster_name = "argocd-terraform-staging"
# - environment = "staging"
# - node_group_instance_types = ["m5.large"]    # Balanced performance
# - single_nat_gateway = false                  # HA setup
# - node_group_desired_size = 3
# - node_group_min_size = 2
# - node_group_max_size = 8
